# Environment variables (now in backend)
backend/.env
backend/.env.local
backend/.env.development.local
backend/.env.test.local
backend/.env.production.local

# Node.js (now in backend)
backend/node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log

# Uploads directory (now in backend)
backend/uploads/
backend/submissions/

# IDEs and editors (usually kept at root, but can be specific)
.idea/
.vscode/ # Keep at root unless you have separate settings
*.swp
*.swo

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Documentation files - Removed *.md as it's too broad.
# Add specific generated documentation files if needed.